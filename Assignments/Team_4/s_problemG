//sami
#include <iostream>
#include <string>
#include<iomanip>
#include <math.h>
#include<algorithm>
using namespace std;

int main() {
string G;
    getline(cin, G);

    string spacesss = "";
    for (int i = 0; i < G.size(); i++) {
        if (isalpha(G[i]) || G[i] == ' ') { 
            spacesss += G[i]; 
        }
    }

    cout << spacesss << endl;*/
}
